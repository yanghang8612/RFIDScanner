package com.casc.rfidscanner.message;

public class OnlineUploadedMessage {

    public boolean isFromDB;

    public OnlineUploadedMessage(boolean isFromDB) {
        this.isFromDB = isFromDB;
    }
}
